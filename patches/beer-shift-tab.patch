diff --git a/crates/beer/src/bindings.rs b/crates/beer/src/bindings.rs
index 6e6588d..d533692 100644
--- a/crates/beer/src/bindings.rs
+++ b/crates/beer/src/bindings.rs
@@ -101,6 +101,9 @@ impl Chord {
       return false;
     }
     event.keysym == self.key
+      // XKB reports Shift+Tab as ISO_Left_Tab rather than Tab.
+      // Modifiers were checked above, so treat the two symbols alike here.
+      || (self.key == Keysym::Tab && event.keysym == Keysym::ISO_Left_Tab)
       || matches!(
           (self.key.key_char(), event.keysym.key_char()),
           (Some(a), Some(b)) if a.eq_ignore_ascii_case(&b)
