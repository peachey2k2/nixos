# Pi global instructions (only relevant when configuring Pi)

- Do not patch, fork, or directly edit installed Pi extension package code.
- Configure extensions through their supported settings files and documented options instead.
- If an extension appears to need a code change, first inspect its documentation/configuration surface and suggest a settings-based solution.
- Treat files under the Pi npm/git package install directories as vendor code.
